name: PDW duplicate filter UI modernize once

on:
  push:
    branches:
      - tmp/filter-duplicate-ui-apply
    paths:
      - .github/workflows/filter-duplicate-ui-modernize-once.yml
      - .github/scripts/filter-duplicate-ui-modernize-once.ps1

permissions:
  contents: write

jobs:
  patch-filter-duplicate-ui:
    runs-on: [self-hosted, Windows, X64]
    steps:
      - name: Checkout exact branch
        uses: actions/checkout@v4
        with:
          ref: tmp/filter-duplicate-ui-apply
          fetch-depth: 0

      - name: Apply bounded duplicate filter presentation patch
        shell: powershell -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: .github/scripts/filter-duplicate-ui-modernize-once.ps1
