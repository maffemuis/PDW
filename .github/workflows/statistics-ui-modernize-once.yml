name: PDW Statistics UI modernize once

on:
  push:
    branches:
      - tmp/statistics-ui-apply
    paths:
      - .github/workflows/statistics-ui-modernize-once.yml
      - .github/scripts/statistics-ui-modernize-once.ps1

permissions:
  contents: write

jobs:
  patch-statistics-ui:
    runs-on: [self-hosted, Windows, X64]
    steps:
      - name: Checkout exact branch
        uses: actions/checkout@v4
        with:
          ref: tmp/statistics-ui-apply
          fetch-depth: 0

      - name: Apply bounded Statistics presentation patch
        shell: powershell -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: .github/scripts/statistics-ui-modernize-once.ps1
