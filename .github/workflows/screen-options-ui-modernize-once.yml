name: PDW Screen Options UI modernize once

on:
  push:
    branches:
      - tmp/screen-options-ui-apply
    paths:
      - .github/workflows/screen-options-ui-modernize-once.yml
      - .github/scripts/screen-options-ui-modernize-once.ps1

permissions:
  contents: write

jobs:
  patch-screen-options-ui:
    runs-on: [self-hosted, Windows, X64]
    steps:
      - name: Checkout exact branch
        uses: actions/checkout@v4
        with:
          ref: tmp/screen-options-ui-apply
          fetch-depth: 0

      - name: Apply bounded Screen Options presentation patch
        shell: powershell -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: .github/scripts/screen-options-ui-modernize-once.ps1
