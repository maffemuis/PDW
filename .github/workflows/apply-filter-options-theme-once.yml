name: Apply Filter Options theme once

on:
  push:
    branches:
      - modernization/windows11-ui
    paths:
      - .github/workflows/apply-filter-options-theme-once.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: [self-hosted, Windows, X64]
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: modernization/windows11-ui
          fetch-depth: 0

      - name: Apply bounded Filter Options theme patch
        shell: cmd
        run: python .github\scripts\apply_filter_options_theme.py

      - name: Commit Filter Options renderer patch
        shell: cmd
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add utils/windows11_ui.cpp
          git commit -m "Theme Filter Options dialog and finish Dutch header"
          git push origin HEAD:modernization/windows11-ui
