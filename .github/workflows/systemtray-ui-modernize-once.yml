name: PDW System Tray UI modernize once

on:
  push:
    branches:
      - tmp/systemtray-ui-apply
    paths:
      - .github/workflows/systemtray-ui-modernize-once.yml
      - .github/scripts/systemtray-ui-modernize-once.ps1

permissions:
  contents: write

jobs:
  patch-systemtray-ui:
    runs-on: [self-hosted, Windows, X64]
    steps:
      - name: Checkout exact branch
        uses: actions/checkout@v4
        with:
          ref: tmp/systemtray-ui-apply
          fetch-depth: 0

      - name: Apply bounded System Tray presentation patch
        shell: powershell -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: .github/scripts/systemtray-ui-modernize-once.ps1
